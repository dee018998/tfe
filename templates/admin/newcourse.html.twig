{% extends 'admin/base.html.twig' %}

{% block title %}Nouveau cours{% endblock %}
{% block body %}
    <section class="col p-3 mt-2">
        <div class="row">
            <h2 class="col-10">Ajoutez un cours</h2>
            <div class="col">
                <a href="{{ path('app_admin_course') }}" class="btn btn-outline-dark">Retour</a>

            </div>
        </div>

            {{ form_start(form) }}

            {{ form_widget(form) }}
            <input type="submit" value="Ajouter" class="btn btn-secondary">
            {{ form_end(form) }}

    </section>
{% endblock %}