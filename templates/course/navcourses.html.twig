
<div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab" tabindex="0">...
    <div class="row ">
        <p>help</p>
        {% for course in courses %}
            {% if (course.isPublished == 1) %}
                <div class="col-12 col-md-3 col-xl-2  border-1   ">
                    <img src="{{ asset('images/courses/' ~ course.image) }}"
                         class="course-img border-primary rounded-2 p-1 bg-warning"
                         alt="...">
                    <p class="text-white 	d-none d-md-block  px-2 "> {{ course.name|capitalize }} </p>
                </div>
            {% endif %}
        {% endfor %}
    </div>
</div>



