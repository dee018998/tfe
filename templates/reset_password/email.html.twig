<h1>Hi</h1>

<h2 class="h5">To reset your password, please visit the following link</h2>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<h2 class="h6">This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</h2>

<h3 class="h6">Online-DJ-school team</h3>
